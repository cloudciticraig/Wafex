{
  "name": "wafex-merchandising-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix client install --no-audit --no-fund && npm --prefix client run build",
    "start": "node server/index.js",
    "dev": "node server/index.js & npm --prefix client run dev"
  },
  "dependencies": {
    "@azure/msal-node": "^5.4.1",
    "compression": "^1.7.5",
    "connect-pg-simple": "^10.0.0",
    "express": "^4.21.2",
    "express-session": "^1.19.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.13.1"
  }
}
